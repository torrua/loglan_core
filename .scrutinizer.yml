checks:
    python:
        code_rating: true
        duplicate_code: true

build:
  environment:
    python: 3.11.5
  nodes:
    tests:
      dependencies:
        before:
          - pip install -r requirements-tests.txt